#include <stdio.h>

struct sensor_data
{
    // accelerometer: floating point three axis (x,y,z)
    // gyro, floating point three axes (x,y,z)
    // temperature, floating point
    // humidity, floating point number
    // brightness: light, 16-bit unsigned integer
    // air pressure: pressure, floating point numbehr
};

int main()
{
    return 0;
}
